后台启动
docker-compose up -d --build
前台启动
# 0
docker build -t my-nextjs-frontend .

# 1. 停掉并移除旧的前端容器
docker stop frontend-app && docker rm frontend-app

# 2. 在正确的网络中重新启动，并注入所有变量
docker run -d \
  --name frontend-app \
  --network global_app_network \
  -p 3000:3000 \
  -e DIFY_API_KEY=app-NBKwaBk6LW95NN9FDu1qBIop \
  -e DIFY_API_BASE=http://docker-api-1:5001/v1 \
  -e DATABASE_URL=postgresql://eitp:eitp@dify-ai-backend-db-1:5432/ai_business_automation_dev \
  -e NEXTAUTH_SECRET=mkeObuNWDbJiZA+AGjXCPiZL5LIJzWn7xeOsp5jaBlQ= \
  -e NEXTAUTH_URL=http://172.207.84.114:3000 \
  my-nextjs-frontend