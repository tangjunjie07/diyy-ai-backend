"""Repository layer stubs for dify-ai-backend.

The original ai-business-automation ingestion-service has a richer repos layer
(e.g., tenant API secrets stored in Postgres with RLS).

For the purposes of migrating `app.account_classifier`, we provide minimal
implementations here so imports resolve.
"""
